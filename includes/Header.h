#pragma once

#include <iostream>
#include <cassert>
#include <vector>
#include <string>
#include <fstream>

using ushint = unsigned short int;
using ushort = unsigned short int;

using shint  = short int;
using uint   = unsigned int;

using ulong  = unsigned long int;
using ulint  = unsigned long int;